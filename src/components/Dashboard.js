// src/components/Dashboard.js

import React from 'react';

const Dashboard = ({ username }) => {
  return (
    <div>
      <h1>Welcome, {username}</h1>
      {/* Add the content for the Dashboard here */}
    </div>
  );
};

export default Dashboard;
